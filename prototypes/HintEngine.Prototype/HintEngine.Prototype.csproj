<Project Sdk="Microsoft.NET.Sdk">

  <!-- PROTOTYPE — throwaway. Standalone on purpose: no COMReference, so plain
       `dotnet build`/`dotnet run` works (unlike the main project). -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>HintEnginePrototype</RootNamespace>
  </PropertyGroup>

</Project>
